import {Grid} from "@mui/material";

const Banner = () => {
    return (
        <Grid container sx={{ 
            position: 'relative',
            background: 'linear-gradient(to right, red, orange)',
            height: '300px',
            top: '84px'
          }}>

        </Grid>
    );
};

export default Banner;