import {Grid} from "@mui/material";

const Banner = () => {
    return (
        <Grid container sx={{ 
            background: 'linear-gradient(to right, red, orange)',
            height: '500px',
          }}>

        </Grid>
    );
};

export default Banner;